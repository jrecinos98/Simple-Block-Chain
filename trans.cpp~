#include <iostream>
#include <string>
#include <cstdlib>
#include "trans.h"


namespace trans_chain{
  Transaction::Transaction(int amount, string sender, string receiver, Transaction *next){
  
  }
  string Transaction::generate_nonce(){
    srand(time(NULL));
    string temp="";
    while(//Last char of temp is not valid)
      {
	temp= char(rand()%26+97);
      }
      return temp;
    //check if nonce is valid.
  }
  string Transaction::getHash(){
  }
  
  
  Blockchain::Blockchain(int amount, string sender, string receiver){
    latest_trans= new Transaction(amount, sender, receiver);
    tail= latest_trans;
  }
  Blockchain::Blockchain(){
    latest_trans= new Transaction();
    tail= latest_trans;
  }
  Blockchain::insert_transaction(int amount, string sender, string receiver){
  }
  Blockchain::find_transactions(string sender){
  }
  Blockchain::verify_transactions(){

  }
  
  
  
}
